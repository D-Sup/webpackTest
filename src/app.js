import { plus } from "./plus.js";
import './style.css'

// console.log(plus(2, 4));
// DOMContentLoaded - DOM 파싱이 완료된 직후 
document.addEventListener('DOMContentLoaded', () => {
  document.body.innerHTML = `<img src="${rabbit}>`;
})